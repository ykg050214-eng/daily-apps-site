# 賃貸更新・引っ越し費用メモ

Googleスプレッドシートの `案番号 2` をもとに作成した、依存なしの静的Webアプリです。

## できること

- 更新費用と引っ越し費用の総額比較
- 契約満了日からの期限逆算
- 比較結果の PNG 保存
- 印刷画面からの PDF 保存

## 起動方法

`index.html` をブラウザで開くだけで動きます。

ローカルサーバーで確認したい場合:

```powershell
python -m http.server 4173
```

その後、`http://localhost:4173` を開いて確認できます。
